[project]
name = "gradiochatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio",
    "openai",
    "python-dotenv"
]

[project.scripts]
chat="cli:chat"
